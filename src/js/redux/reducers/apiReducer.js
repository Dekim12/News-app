import {} from '../../constants';

const initialState = {};

const apiState = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default apiState;
